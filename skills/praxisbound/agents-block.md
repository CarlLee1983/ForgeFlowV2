For implementation requests, locate the repository root and read that root's
`.agents/skills/praxisbound/SKILL.md`. General questions do not start a Story
workflow. `$praxisbound` is the explicit fallback. If the local skill is missing
or conflicts with instructions, report the concrete issue and proposed repair.
